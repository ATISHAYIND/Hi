find / -name "server_cert" 2>/dev/null
ls -la /etc/cohesity*/ 2>/dev/null
ls -la /opt/cohesity/agent/ 2>/dev/null

ps -ef | grep -oP '(?<=--linux_agent_cert_file_path=)\S+'


grep -oP '(?<=certificate: ")[^"]*' <PATH> | head -1 | sed 's/\\n/\n/g' | openssl x509 -noout -issuer -subject -dates
